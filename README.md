# AMP
Anti-Meta Patcher, a tool for removing proprietary Meta APIs from dumped APKs.

This project aims to convert proprietary APKs that cannot run properly on a generic OpenXR implementation (eg. Monado) into a generic APK that can be run on any OpenXR runtime.
